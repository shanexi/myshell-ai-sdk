"你的预期已经清楚记录：菜单的翻译及百科/科普结果，要用 **Markdown 表格**的形式统一渲染和展示。

你的最终要求为：

- 菜品原文显示原语言字符；
- 菜品中文翻译、30字以内百科介绍、30字以内点餐建议、菜品图片都作为表格列；
- “示例图片”采用 `![](图片URL)` Markdown 方式插入；
- 表格整体为标准Markdown表格，支持一键复制，无需其他格式或特殊UI。

---

### ShellAgent 方案说明与修正措施

**1. 流程节点/输出渲染说明**

- 在Bot的最后输出阶段，自动将所有结构化信息生成符合Markdown语法的表格字符串，并作为主内容输出；
- 表格内容包括：原文 | 中文 | 科普简介 | 点餐建议 | 示意图片（Markdown形式）。

**2. 技术实现方式**

- 如果当前App结果尚未以Markdown表格格式输出，可以直接在终态的主表现层加一个“JSON→Markdown表格”步骤（ShellAgent已有此类处理能力，必要时可用“JSON to Table”或自定义渲染函数）。
- 也可以通过GPTWidget或自定义解析代码，将结构化菜单输出转换为Markdown表格字符串。

---

### 下一步处理

我将为你的Bot在流程终点增加“结构化菜单Json信息自动转markdown表格字符串”的处理，保证你每次点餐结果都以标准Markdown表格形式收到，方便复制使用和在多数App/平台直接显示、粘贴。

---

**最后确认：**  
你是否接受输出如下形式？

```
| 原文 | 中文 | 简介 | 建议 | 示意图 |
|------|------|------|------|--------|
| Spaghetti Carbonara | 卡邦尼意面 | 培根奶酱经典，口感浓郁 | 建议热吃 | ![](示意图url) |
```

如确认，我将为你的应用增加这一最终表格渲染，并确保每次都是Markdown标准格式输出。请回复“确认”或补充进一步约束。"
