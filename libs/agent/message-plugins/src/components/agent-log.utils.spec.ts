import {
  msg1,
  msg2,
  msg3,
  msg4,
  msg5,
  msg6,
} from './__storybook_data__/backend_message';

it('test', () => {
  const msgs = [msg1, msg2, msg3, msg4, msg5, msg6];
  // 先有一个假设
  // agent_log 同一个 message id 必须保持
  // 如果不保持，我也认为 append 到同一个
});
