# simple-services

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build simple-services` to build the library.

## Running unit tests

Run `nx test simple-services` to execute the unit tests via [Jest](https://jestjs.io).
