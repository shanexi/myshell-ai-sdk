# figma-twcfg

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build figma-twcfg` to build the library.

## Running unit tests

Run `nx test figma-twcfg` to execute the unit tests via [Jest](https://jestjs.io).
