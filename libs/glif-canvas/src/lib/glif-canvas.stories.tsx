import type { Meta } from '@storybook/react';
import { GlifCanvas } from './glif-canvas';

const Story: Meta<typeof GlifCanvas> = {
  component: GlifCanvas,
  title: 'GlifCanvas',
};
export default Story;

export const Primary = {
  args: {},
};
