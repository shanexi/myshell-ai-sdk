# def

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build def` to build the library.

## Running unit tests

Run `nx test def` to execute the unit tests via [Jest](https://jestjs.io).
