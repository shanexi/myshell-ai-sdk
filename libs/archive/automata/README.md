# archive-automata

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build archive-automata` to build the library.

## Running unit tests

Run `nx test archive-automata` to execute the unit tests via [Jest](https://jestjs.io).
