# biz-def

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build biz-def` to build the library.

## Running unit tests

Run `nx test biz-def` to execute the unit tests via [Jest](https://jestjs.io).
