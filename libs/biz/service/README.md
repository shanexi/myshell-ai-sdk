# biz-service

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build biz-service` to build the library.

## Running unit tests

Run `nx test biz-service` to execute the unit tests via [Jest](https://jestjs.io).
