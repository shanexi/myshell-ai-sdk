# common-def

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build common-def` to build the library.

## Running unit tests

Run `nx test common-def` to execute the unit tests via [Jest](https://jestjs.io).
