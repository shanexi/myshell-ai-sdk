import React from 'react';
type P = {
    isFullscreen: boolean;
    onClick: (event: any) => void;
};
declare const Fullscreen: React.MemoExoticComponent<({ isFullscreen, onClick }: P) => import("react/jsx-runtime").JSX.Element>;
export default Fullscreen;
