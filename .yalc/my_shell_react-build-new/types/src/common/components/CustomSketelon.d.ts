import { ReactNode } from 'react';
interface CustomSketelonProps {
    customClass?: string;
    children?: ReactNode;
    animate?: boolean;
}
export default function CustomSketelon({ customClass, children, animate }: CustomSketelonProps): import("react/jsx-runtime").JSX.Element;
export {};
