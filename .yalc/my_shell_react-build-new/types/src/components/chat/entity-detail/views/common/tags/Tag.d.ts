type P = {
    showHoverableContent?: boolean;
    hoverContent?: React.ReactNode | string;
    iconUrl?: string;
    label: string;
    className?: string;
};
export default function Tag({ showHoverableContent, hoverContent, iconUrl, label, className }: P): import("react/jsx-runtime").JSX.Element;
export {};
