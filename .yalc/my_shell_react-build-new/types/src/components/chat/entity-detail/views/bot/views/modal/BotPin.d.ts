type P = {
    id: string;
    pinned: boolean;
    setPinned: (val: boolean) => void;
};
export default function BotPin({ id, pinned, setPinned }: P): import("react/jsx-runtime").JSX.Element;
export {};
