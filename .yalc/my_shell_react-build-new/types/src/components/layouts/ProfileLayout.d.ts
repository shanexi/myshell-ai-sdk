import { ReactNode } from 'react';
type Props = {
    children: ReactNode;
};
declare function ProfileLayout({ children }: Props): import("react/jsx-runtime").JSX.Element;
export default ProfileLayout;
