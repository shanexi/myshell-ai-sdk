interface RewardsInviteCardProps {
    title: string;
    answer: string;
    count: number;
}
export default function RewardsInviteCard({ title, answer, count }: RewardsInviteCardProps): import("react/jsx-runtime").JSX.Element;
export {};
