interface P {
    columns: number;
    num?: number;
    showBtn?: boolean;
    animate?: boolean;
}
export default function TableSkeletonMobile({ columns, num, showBtn, animate }: P): import("react/jsx-runtime").JSX.Element;
export {};
