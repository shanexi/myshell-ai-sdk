import { DisplayMessage } from '../../../../../../chat-new/model/definitions';
type P = {
    message?: DisplayMessage;
    showText?: boolean;
    showAudio?: boolean;
};
export default function AsyncJobDisplay({ message, showText, showAudio }: P): import("react/jsx-runtime").JSX.Element;
export {};
;
export {};
