{
  "type": "module",
  "scripts": {
    "dev2": "vite",
    "dev": "wrangler dev",
    "build": "vite build --mode client && vite build",
    "preview": "wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "private": true
}
